<h1>Editing Trip #<%= @trip.id %></h1>


<%= form_tag(trip_url(@trip), method: 'patch') do %>

  <div>
    <%= label_tag :trip_name %><br />
    <%= text_field_tag :trip_name, @trip.trip_name %>
  </div>


  <div>
    <%= submit_tag "Update Trip", class: "btn btn-success " %>
  </div>

<% end %>
